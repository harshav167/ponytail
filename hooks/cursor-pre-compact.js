#!/usr/bin/env bun
process.stdout.write(JSON.stringify({
  user_message: 'Ponytail still applies after compaction: YAGNI, stdlib/native first, fewest files, one runnable check for non-trivial logic.',
}));
